{
  "name": "tobi-dev-portfolio",
  "version": "1.0.0",
  "scripts": {
    "start": "your-start-command",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {}
}
